- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.dotfiles: ''
    ~/bin:
    ~/.aliases:
    ~/.bashrc:
    ~/.config/nvim:
    ~/.dircolors:
    ~/.editorconfig:
    ~/.exports:
    ~/.functions:
    ~/.gitattributes_global:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.inputrc:
    ~/.profile:
    ~/.psqlrc:
    ~/.ssh:
    ~/.tmux.conf:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
