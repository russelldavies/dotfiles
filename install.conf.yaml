- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.aliases:
    ~/.bashrc:
    ~/.config/nvim:
    ~/.dircolors:
    ~/.editorconfig:
    ~/.exports:
    ~/.functions:
    ~/.gitattributes_global:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.hammerspoon:
    ~/.inputrc:
    ~/.profile:
    ~/.psqlrc:
    ~/.ssh:
    ~/.tmux.conf:
    ~/bin:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - ['chmod -R u=rwX,go= ssh', Fixing ssh key permissions]
